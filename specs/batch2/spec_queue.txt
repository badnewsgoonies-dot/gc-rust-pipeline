Write a complete Rust 2021 source file implementing VQueue<T: Clone> backed by std::collections::VecDeque.
Required: #[derive(Debug, Clone)] on struct.
pub struct VQueue<T: Clone> { data: std::collections::VecDeque<T> }

impl<T: Clone> VQueue<T>, numbered:
(1) pub fn new() -> Self — data is empty VecDeque.
(2) pub fn enqueue(&mut self, value: T) — self.data.push_back(value).
(3) pub fn dequeue(&mut self) -> Option<T> — self.data.pop_front().
(4) pub fn front(&self) -> Option<&T> — self.data.front().
(5) pub fn len(&self) -> usize.
(6) pub fn is_empty(&self) -> bool.

impl<T: Clone> Default for VQueue<T> returning Self::new().

#[cfg(test)] mod tests using VQueue<i64>:
(1) fn enqueue_dequeue_fifo: new, enqueue 10, enqueue 20, enqueue 30, assert dequeue Some(10), assert dequeue Some(20), assert len 1.
(2) fn front_does_not_consume: new, enqueue 5, assert front Some(&5), assert front Some(&5), assert len 1.
(3) fn empty_dequeue: new VQueue::<i64>, assert dequeue None, assert is_empty().

Std only. Output ONLY the Rust source code. No markdown fences, no prose.
