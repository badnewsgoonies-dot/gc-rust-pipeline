Write a complete Rust 2021 source file implementing Stack<T: Clone>.
Required: #[derive(Debug, Clone, PartialEq)] on struct.
pub struct Stack<T: Clone> { data: Vec<T> }

impl<T: Clone> Stack<T>, numbered:
(1) pub fn new() -> Self — data empty.
(2) pub fn push(&mut self, value: T) — self.data.push(value).
(3) pub fn pop(&mut self) -> Option<T> — self.data.pop().
(4) pub fn peek(&self) -> Option<&T> — self.data.last(). Must borrow.
(5) pub fn len(&self) -> usize.
(6) pub fn is_empty(&self) -> bool.

impl<T: Clone> Default for Stack<T> returning Self::new().

#[cfg(test)] mod tests using Stack<i64>:
(1) fn push_pop_lifo: new, push 1, push 2, push 3, assert pop Some(3), assert pop Some(2), assert len 1.
(2) fn peek_does_not_consume: new, push 42, assert peek Some(&42), assert peek Some(&42), assert len 1.
(3) fn empty_ops: new Stack::<i64>, assert is_empty(), assert pop None, assert peek None.

Std only. Output ONLY the Rust source code. No markdown fences, no prose.
